{
  "name": "best-library",
  "version": "0.1.0",
  "description": "Best library of javascript",
  "scripts": {
    "test": "mocha test/spec/best-library-spec.js",
    "build": "mkdir -p ./dist && uglifyjs ./src/best-library.js > ./dist/best-library.min.js",
    "preversion": "npm test",
    "version": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AstR0x/best-library.git"
  },
  "license": "MIT",
  "devDependencies": {
    "mocha": "^5.2.0",
    "uglify-js": "^3.4.9"
  },
  "dependencies": {
    "chai": "^4.2.0"
  }
}
